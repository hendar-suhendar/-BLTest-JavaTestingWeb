# Crete Maven project and update pom.xml
Cucumber Java
Cucumber Junit
Junit
Selenium Java

#Create Folder Structure
@Project Name
   -Features(folder)
   -pageObjects(Package)
   -stepDefinitions
   -utilities(package)
   -Drivers(Folder)
   -Terget(Reports)
   -pom.xml
